interface LobbyPlayerCardProps {
    profilePicture: string;
    username: string;
}

export const LobbyPlayerCard = ({ profilePicture, username }: LobbyPlayerCardProps) => {
    return (
        <div>

        </div>
    )
}